name: Security Analysis

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  asan_build_and_test:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Build Target Nginx Container
      run: |
        docker compose build target
      
    - name: Run Fuzzer Tests against Target
      run: |
        docker compose up -d target
        docker compose up fuzzer
        docker compose down

    - name: Check ASAN/UBSAN Logs
      run: |
        echo "Validating logs for bounds-checking bypass..."
        if grep -q "ERROR: AddressSanitizer" target_logs/asan.*; then
          echo "Vulnerability detected! Defense patch failed."
          exit 1
        else
          echo "System is secure. No memory corruption found."
        fi
